name: Keep Neon Alive

on:
  schedule:
    - cron: '*/4 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend health endpoint
        run: curl -sf "${{ secrets.BACKEND_HEALTH_URL }}" || echo "Ping failed"
